import{_ as a,o as t,c as s,ai as o}from"./chunks/framework.lvpqbiZd.js";const u=JSON.parse('{"title":"Site Update Recovery","description":"","frontmatter":{},"headers":[],"relativePath":"code/site-update/index.md","filePath":"code/site-update/index.md"}'),i={name:"code/site-update/index.md"};function n(r,e,d,c,l,p){return t(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="site-update-recovery" tabindex="-1">Site Update Recovery <a class="header-anchor" href="#site-update-recovery" aria-label="Permalink to “Site Update Recovery”">​</a></h1><p>When a site update (a <code>Pull</code> or <code>Migrate</code> deploy onto a new bench) fails, Press tries to put the site back the way it was before the update. This page covers that recovery flow — what runs automatically, the one-shot table restore Press falls back to, and when it gives up and asks a human to step in.</p><p>The logic lives in the <strong>Site Update</strong> doctype (<code>press/press/doctype/site_update/site_update.py</code>) and the <strong>Site</strong> doctype (<code>press/press/doctype/site/site.py</code>).</p><h2 id="the-happy-path-fails" tabindex="-1">The happy path fails <a class="header-anchor" href="#the-happy-path-fails" aria-label="Permalink to “The happy path fails”">​</a></h2><p>An update runs as an <code>Update Site Pull</code> or <code>Update Site Migrate</code> agent job. When that job fails (<code>handle_failure</code>):</p><ol><li>The site is marked <code>Broken</code>.</li><li>If the update was run <strong>with backups</strong> (the normal case), a recovery job is triggered to move the site back to its source bench.</li><li>If the update was run <strong>with backups skipped</strong>, recovery is impossible — there is nothing to roll back to. The Site Update goes straight to <code>Fatal</code> and the user is notified to fix the site over SSH (see <a href="#skipped-backups">Skipped backups</a>).</li></ol><h2 id="recovery" tabindex="-1">Recovery <a class="header-anchor" href="#recovery" aria-label="Permalink to “Recovery”">​</a></h2><p><code>trigger_recovery_job</code> moves the site back to its source bench (<code>Recover Failed Site Pull</code> / <code>Recover Failed Site Migrate</code>) or, if the site never left the source bench, just disables maintenance mode (<code>Recover Failed Site Update</code>).</p><h3 id="bumping-max-statement-time-before-a-migrate-recovery" tabindex="-1">Bumping <code>max_statement_time</code> before a migrate recovery <a class="header-anchor" href="#bumping-max-statement-time-before-a-migrate-recovery" aria-label="Permalink to “Bumping max_statement_time before a migrate recovery”">​</a></h3><p>Recovering a <code>Migrate</code> runs heavy restore/migrate queries. On large sites these can exceed the database server&#39;s <code>max_statement_time</code> and get killed mid-query, turning a recoverable failure into a fatal one.</p><p>To avoid that, before triggering a recovery <strong>migrate</strong> job for a large database (over <code>STATEMENT_TIME_BUMP_SIZE_MB</code>, 2 GB), Press proactively increases <code>max_statement_time</code> on the database server by one hour (<code>SiteUpdate.bump_max_statement_time_before_recovery</code> → <code>Site.increase_max_statement_time</code>). <code>max_statement_time</code> is a dynamic MariaDB variable stored as a row in the Database Server&#39;s variables child table, so the change applies without a restart. The bump is recorded as a comment on the Site Update. Smaller databases finish well within the timeout and are skipped.</p><p>The pre-bump value is stashed on the Site Update (<code>previous_max_statement_time</code>) and <strong>restored once recovery finishes</strong> (<code>restore_max_statement_time</code>), at the recover job&#39;s terminal state. Otherwise the value would ratchet up an hour on every recovery.</p><h3 id="when-recovery-fails" tabindex="-1">When recovery fails <a class="header-anchor" href="#when-recovery-fails" aria-label="Permalink to “When recovery fails”">​</a></h3><p>If the recovery job fails, the Site Update goes <code>Fatal</code>, the site is marked <code>Broken</code>, and <code>Site.fatal_site_update</code> is set to the failed update.</p><p>The site is left for a human. A migrate recovery that fails after its &quot;Move Site&quot; step has moved the site back to the source bench leaves only the table restore undone; re-running the whole recovery would fail at &quot;Move Site&quot;, because the agent&#39;s <code>move_site</code> is not idempotent (the site directory is no longer on the destination bench). The operator restores just the tables with the <strong>Restore Tables</strong> button (see below).</p><h2 id="restoring-tables-after-a-fatal-update" tabindex="-1">Restoring tables after a fatal update <a class="header-anchor" href="#restoring-tables-after-a-fatal-update" aria-label="Permalink to “Restoring tables after a fatal update”">​</a></h2><p>Restoring a site&#39;s tables from its backup (<code>Site.restore_tables</code>, a <code>Restore Site Tables</code> agent job) is what brings a <code>Fatal</code> site back up. The dashboard shows a <strong>Restore Tables</strong> banner and action on a <code>Broken</code> site with a <code>fatal_site_update</code>. The button calls <code>Site.retry_restore_tables</code>, which refuses unless the fatal update is still the site&#39;s latest, no <code>Restore Site Tables</code> job is already running, and <strong>MariaDB reports itself up</strong> (<code>DatabaseServer.is_mariadb_up</code>, reading mysqld_exporter&#39;s <code>mysql_up</code> metric). The restore gets one attempt, so a server with no metric — a monitoring outage included — counts as down and the operator retries later.</p><p>The metric is read with <code>prometheus_instant_value</code> (<code>/api/v1/query</code>), not <code>prometheus_query</code>, whose range samples can be a timegrain (120s) stale — too old to decide whether a server is up right now.</p><p>The job is handled by <code>process_restore_tables_job_update</code>:</p><ul><li><strong>On success</strong>, the site is reactivated. If it was stuck on a fatal update, that update <strong>stays <code>Fatal</code></strong> but its cause of failure is marked resolved (<code>set_cause_of_failure_is_resolved</code>, which also clears the site&#39;s <code>fatal_site_update</code>). The site is usable again; the update itself still failed and is recorded as such.</li><li><strong>On failure</strong>, the site stays <code>Broken</code> and <code>fatal_site_update</code> remains set. Statement timeouts are avoided proactively by the one-hour <code>max_statement_time</code> bump done before the recovery migrate (see above), so there is no per-attempt retry loop here.</li></ul><h2 id="skipped-backups" tabindex="-1">Skipped backups <a class="header-anchor" href="#skipped-backups" aria-label="Permalink to “Skipped backups”">​</a></h2><p>If a user runs an update with backups skipped and it fails, the database may be left partially migrated with no backup to restore from. The site cannot be recovered automatically.</p><p>In this case the failure notification (<code>agent_job_notifications.py</code>) is replaced with an actionable message — <em>&quot;Site update failed and cannot be recovered automatically&quot;</em> — telling the user to connect over SSH and fix the site manually.</p><h2 id="status-lifecycle" tabindex="-1">Status lifecycle <a class="header-anchor" href="#status-lifecycle" aria-label="Permalink to “Status lifecycle”">​</a></h2><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>Update fails</span></span>
<span class="line"><span>   │</span></span>
<span class="line"><span>   ├─ skipped backups ──────────────► Fatal  (SSH notification)</span></span>
<span class="line"><span>   │</span></span>
<span class="line"><span>   └─ with backups</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>     Recovering ──► (recover job)</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ├─ recover success ─────────► Recovered</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        └─ recover failure ─────────► Fatal  (site Broken, fatal_site_update set)</span></span>
<span class="line"><span>                                          │</span></span>
<span class="line"><span>                              operator clicks Restore Tables</span></span>
<span class="line"><span>                              (needs MariaDB up: mysql_up)</span></span>
<span class="line"><span>                                          │</span></span>
<span class="line"><span>                                   ┌──────┴──────┐</span></span>
<span class="line"><span>                                failure       success</span></span>
<span class="line"><span>                                   │             │</span></span>
<span class="line"><span>                                   ▼             ▼</span></span>
<span class="line"><span>                                Broken    Fatal, cause resolved</span></span>
<span class="line"><span>                           (fatal_site_update (site Active again,</span></span>
<span class="line"><span>                                remains set)  fatal_site_update cleared)</span></span></code></pre></div><p>(In all terminal cases the recovery-migrate <code>max_statement_time</code> bump is restored to its pre-bump value.)</p><h2 id="key-constants" tabindex="-1">Key constants <a class="header-anchor" href="#key-constants" aria-label="Permalink to “Key constants”">​</a></h2><table tabindex="0"><thead><tr><th>Constant</th><th>Value</th><th>Meaning</th></tr></thead><tbody><tr><td><code>STATEMENT_TIME_INCREMENT</code></td><td>3600</td><td>Seconds (one hour) <code>max_statement_time</code> is bumped by before a recovery migrate on large sites.</td></tr><tr><td><code>STATEMENT_TIME_BUMP_SIZE_MB</code></td><td>2048</td><td>DB size (MB) above which the <code>max_statement_time</code> bump is applied. Not to be confused with <code>LARGE_DATABASE_SIZE_MB</code> (102400), which blocks a logical-backup update outright.</td></tr><tr><td><code>DEFAULT_MAX_STATEMENT_TIME</code></td><td>3600</td><td>Assumed <code>max_statement_time</code> when it isn&#39;t set on the database server.</td></tr></tbody></table>`,28)])])}const b=a(i,[["render",n]]);export{u as __pageData,b as default};
