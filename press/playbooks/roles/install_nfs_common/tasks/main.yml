---
- name: Install NFS Common
  become: yes
  become_user: root
  apt:
    name: nfs-common
    state: present
    update_cache: yes

  register: result
  until: result.failed == false
  retries: 5
  delay: 120