# arm-builds

Dockerfiles for dependencies that publish no arm64 image. Build these by hand
and push them to the registry when Press runs benches on ARM servers.

- `RedisearchARMBuild` — RediSearch 2.4.6 on Redis 6.2, built for arm64
